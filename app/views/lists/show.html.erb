<h1><%= @list.name %></h1>
<%= link_to "Add a bookmark", new_list_bookmark_path(@list) %>
<hr>
<% @list.bookmarks.each do |bookmark| %>
  <p><%= bookmark.movie.title %> - <%= link_to "Delete", bookmark_path(bookmark), method: :delete, class: "btn btn-danger" %></p>
<% end %>
